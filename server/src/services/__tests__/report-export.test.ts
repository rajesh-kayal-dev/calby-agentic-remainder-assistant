import test from "node:test";
import assert from "node:assert";

test("Google Docs export service loads", () => {
  assert.ok(true);
});

test("Google Sheets export service loads", () => {
  assert.ok(true);
});
