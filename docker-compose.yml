services:
  postgres:
    image: postgres:16-alpine
    container_name: calby-postgres
    restart: unless-stopped
    ports:
      - "5442:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: calby_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

